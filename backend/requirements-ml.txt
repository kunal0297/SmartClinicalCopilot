# Optional heavy AI/ML dependencies for Smart Clinical Copilot.
#
# The application runs fully without these — clinical rule matching, alerts and
# template-based explanations all work out of the box. Install this file only if
# you want:
#   * SHAP-based feature-importance explanations (shap)
#   * Local HuggingFace LLM inference (transformers, torch)
#   * OpenAI-powered narrative explanations (openai)
#   * Local Ollama models (ollama)
#
# Usage:  pip install -r requirements-ml.txt

shap>=0.44.0
transformers>=4.38.0
torch>=2.2.0
openai>=1.12.0
ollama>=0.1.7

# InterSystems IRIS driver (only needed for real IRIS integration)
intersystems-irispython>=3.2.0
