# Smart Clinical Copilot — core backend dependencies
# These are everything required to run the FastAPI backend with graceful
# degradation of external services. Heavy/optional AI dependencies (torch,
# transformers, shap) live in requirements-ml.txt so the base install stays
# lightweight and fast.

# --- Web framework -------------------------------------------------------
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic>=2.5.0
pydantic-settings>=2.1.0

# --- Config / IO ---------------------------------------------------------
python-dotenv>=1.0.0
pyyaml>=6.0.1
aiofiles>=23.2.1
python-dateutil>=2.8.2

# --- HTTP clients --------------------------------------------------------
httpx>=0.26.0
aiohttp>=3.9.0
requests>=2.31.0

# --- Database ------------------------------------------------------------
sqlalchemy>=2.0.0
# psycopg2-binary is only needed for PostgreSQL; SQLite (default) needs nothing.
psycopg2-binary>=2.9.9

# --- Caching / resilience ------------------------------------------------
redis>=5.0.0
cachetools>=5.3.2
tenacity>=8.2.0

# --- Healthcare ----------------------------------------------------------
fhirclient>=4.1.0

# --- Monitoring / logging ------------------------------------------------
prometheus-client>=0.19.0
prometheus-fastapi-instrumentator>=6.1.0
python-json-logger>=2.0.7
structlog>=23.2.0

# --- Security ------------------------------------------------------------
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
pyjwt>=2.8.0

# --- Utilities -----------------------------------------------------------
numpy>=1.26.0
click>=8.1.7
rich>=13.7.0
tabulate>=0.9.0
psutil>=5.9.0

# --- Testing -------------------------------------------------------------
pytest>=7.4.0
pytest-asyncio>=0.23.0
